@model Men

<h1>Delete Men</h1>

<form method="POST" asp-controller="Men" asp-action="ConfirmDelete" asp-route-id="@Model.Id">
    <div class="form-group">
        <p>Are you sure to remove Men '@Model.Name'?</p>
    </div>
    
    <div class="form-group">
        <a asp-controller="Men" asp-action="List" class="btn btn-default">Undo</a>
        <input type="submit" class="btn btn-danger" value="Confirm" />
    </div>
</form>